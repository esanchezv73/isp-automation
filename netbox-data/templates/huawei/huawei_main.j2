system-view
    set hostname "{{ device.name }}"
end

{# --- Interfaces: iterar aquí --- #}
{% for intf in device.interfaces.all() | selectattr('enabled') %}
{% include 'netbox-data/templates/huawei/huawei_interfaces.j2' %}
{% endfor %}
