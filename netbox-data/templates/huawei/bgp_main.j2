! BGP Configuration for {{ device.name }}
  bgp {{ device.cf.['local_asn'] }}
  router-id {{ device.primary_ip4.address.ip }}
{% for session in device.bgp_sessions.all() %}
  neighbor {{ session.remote_ip.address.ip }} remote-as {{ session.remote_as.asn }}
  description {{ session.description }}
  ! Add more session parameters like activate, soft-reconfiguration, etc.
{% endfor %}
! End of BGP config
