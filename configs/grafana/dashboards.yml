apiVersion: 1

providers:
  
 - name: bgp-failover
    orgId: 1
    type: file
    updateIntervalSeconds: 30  # Actualizar cada 30 segundos
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      
      
